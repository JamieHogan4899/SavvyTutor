<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MainTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:textColorPrimary">@color/blackText</item>
        <item name="android:textColorSecondary">@color/blackText</item>
        <item name="android:statusBarColor">@color/blackText</item>
    </style>

    <style name="spinnerTheme" parent="android:Theme">
        <item name="android:textColor">@color/hintColor</item>
    </style>




</resources>